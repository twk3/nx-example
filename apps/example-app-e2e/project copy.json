{
  "name": "example-app-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/example-app-e2e/src",
  "implicitDependencies": ["example-app"],
  "// targets": "to see all targets run: nx show project example-app-e2e --web",
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "config": "apps/example-app-e2e/playwright.config.ts",
        "commands": [
          {
            "command": "npx playwright test --list --project=chromium"
          },
          {
            "command": "CURRENTS_API_URL=https://cy-staging.currents.dev npx pwc --key AcceNBS8GCEFdr4C --project-id ghc2jY --ci-build-id 100109 --project=chromium"
          },
          {
            "command": "sleep 120"
          },
          {
            "command": "npx playwright test --list --project=firefox"
          },
          {
            "command": "CURRENTS_API_URL=https://cy-staging.currents.dev npx pwc --key AcceNBS8GCEFdr4C --project-id ghc2jY --ci-build-id 100109 --project=firefox"
          }
        ]
      }
    }
  }
}
